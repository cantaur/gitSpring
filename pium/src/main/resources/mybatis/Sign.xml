<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.pium.mapper.SignMapper">

    <insert id="signup" parameterType="SignDTO">
        {
            call
            declare
            begin
            insert into member(member_email, member_pw, member_platform) values(#{member_email}, #{member_pw}, #{member_platform});
            insert into AUTHORITIES values(#{member_email}, 'USER');
            end
            }
    </insert>

    <select id="emailCheck" resultType="int" parameterType="String">
        select COUNT(member_email) from member where member_email = #{member_email}
    </select>

    <insert id="emailAuth" parameterType="map">
        insert into email_auth values(#{member_email}, #{uuid})
    </insert>

    <update id="verification" parameterType="map">
        update member set ENABLED = 1 where member_email = (select member_email from email_auth where uuid = #{uuid})
    </update>


</mapper>