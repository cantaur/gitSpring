  < 비디오 패킷 경로 >


1. 요청(클 ->서) 
클라이언트 웹브라워져 ->클릭(요청) -> 패킷 생성 -> LAN 
 -> 라우터 -> 라우터스위치 -> 프록시서버 
   -> 방화벽 -> 라우터 -> 인터넷(WAN) -> 방화벽(80/25포트) 
      -> (프록시서버) -> 웹서버(패킷을 해독/패킷은 재사용)

2. 응답(서 -> 클)
웹서버 --> 응답(패킷) --> 역방향 경로(위의 경로 참고) -> 클라이언트 웹브라우져 



  cf)패킷 : 데이터의 전송단위( 128 byte 해더/데이터/테일러로 구성 )   