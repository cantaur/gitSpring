<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spring.boot.backend.mapper.MybatisMapper">

    <select id="selectMybatis" resultType="MybatisDTO">
        select 'springboot' as name from DUAL
    </select>

</mapper>